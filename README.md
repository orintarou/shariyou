Aim is to couple NextJS/Tailwind Application with a Python backend using Google ADK to promote a conversation with a Gemini Agent.

##Current running methods
1. Navigate into shariyou_js/shariyou.
2. run `npm install`
3. run `next npx dev`

Must have the ability to use the `npx` command to run the site locally (localhost:3000)!
